export type PostPreviewType = {
  id: number,
  title: string,
  body: string,
  imageUrl?: string,
  category: string,
  username: string,
  date: string,
  likeCount: number,
  commentCount: number,
}